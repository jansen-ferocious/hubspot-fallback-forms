# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.sublime-*

# Build artifacts
*.zip

# Logs
*.log

# NOTE: do NOT ignore vendor/ — the bundled Plugin Update Checker library
# ships its own vendor/ (Parsedown, readme parser) that must be committed so
# update downloads are complete.
